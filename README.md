# apc-in-c
APC in C using Data Structures – Implementation of arbitrary precision arithmetic with linked lists, including validation and core operations (add, sub, mul, div) for large number processing in C.
